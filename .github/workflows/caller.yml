name: Call Reusable Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/reusable-build.yml
    with: 
      app_name: my-app
      environment: production

    secrets:
      docker_token: ${{ secerts.DOCKER_TOKEN }}
