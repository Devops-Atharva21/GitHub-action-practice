name: Hello

on:
  workflow_dispatch: 

jobs:
    greet: 
       runs-on: ubuntu-latest
       steps: 
         - name: Says hello to users
           run : echo "Hello Dosto"
         - name: Says Hello to user
           run : echo "Hello $user"
           
